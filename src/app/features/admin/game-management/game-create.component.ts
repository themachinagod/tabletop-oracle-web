import { Component } from '@angular/core';

/**
 * Game creation form stub.
 *
 * Will provide a form for curators to create new games with metadata.
 * Full implementation in a later task.
 *
 * Route: /admin/games/new
 */
@Component({
  selector: 'app-game-create',
  standalone: true,
  template: `
    <div class="stub-page">
      <h1>Create Game</h1>
      <p>Game creation form — implementation pending.</p>
    </div>
  `,
})
export class GameCreateComponent {}

import { Component } from '@angular/core';

/**
 * Admin game detail view stub.
 *
 * Will display game detail with tabs for overview, documents, and
 * expansions. Full implementation in a later task.
 *
 * Route: /admin/games/:gameId
 */
@Component({
  selector: 'app-admin-game-detail',
  standalone: true,
  template: `
    <div class="stub-page">
      <h1>Game Detail</h1>
      <p>Game detail view with tabs — implementation pending.</p>
    </div>
  `,
})
export class AdminGameDetailComponent {}

import { Component } from '@angular/core';

/**
 * Admin home view stub — game library management entry point.
 *
 * Will display the game list with search, filter, and create actions.
 * Full implementation in a later task.
 *
 * Route: /admin
 */
@Component({
  selector: 'app-admin-home',
  standalone: true,
  template: `
    <div class="stub-page">
      <h1>Game Library</h1>
      <p>Game management view — implementation pending.</p>
    </div>
  `,
})
export class AdminHomeComponent {}

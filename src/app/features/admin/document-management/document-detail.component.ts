import { Component } from '@angular/core';

/**
 * Document detail view stub.
 *
 * Will display document metadata, processing status, content preview,
 * and version history. Full implementation in a later task.
 *
 * Route: /admin/games/:gameId/documents/:documentId
 */
@Component({
  selector: 'app-document-detail',
  standalone: true,
  template: `
    <div class="stub-page">
      <h1>Document Detail</h1>
      <p>Document detail view — implementation pending.</p>
    </div>
  `,
})
export class DocumentDetailComponent {}

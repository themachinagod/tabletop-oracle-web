import { Routes } from '@angular/router';

/** Player-facing feature routes. */
export const PLAYER_ROUTES: Routes = [
  {
    path: '',
    children: [
      // Player feature routes will be added during implementation
    ],
  },
];

# Frontend environment configuration
# Build-time config is in src/environments/
# This file documents deployment-time variables

# API_URL — set in environment.prod.ts or via config.json
# Default: /api/v1 (same-origin via reverse proxy)
